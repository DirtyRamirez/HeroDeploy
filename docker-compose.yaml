version: '3.8'
name: academy
services:
  quotes:
    image: quotes
    container_name: quotes
#    build:
#      context: .
#      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    networks:
      - default
  hero:
    image: hero
    container_name: hero
#    build:
#      context: .
#      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    networks:
      - default

networks:
  myNetwork:
    driver: "bridge"